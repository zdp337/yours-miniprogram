---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-03-04T09:03:35.953Z
provider: 
---

# 项目开发历史记录

## 说明
每次开发输出时，必须在此文件中记录：哪位成员做了什么工作、修改/新建了哪些文件。
AI Agent 在每次开发任务完成后，**必须更新此文件**，追加本次迭代的工作记录。

---

## 迭代 1：基础架构 + 用户体系（Yours·凝刻）

> 产品：Yours·凝刻 — AI 个性化 Q 版 3D 手办定制微信小程序
> 技术栈：微信小程序原生+TS+TDesign，后端 Koa2+MySQL(Prisma)+Redis

### PM（组长）工作
- 项目架构设计：微信小程序原生 + Koa2 后端
- 输出迭代1开发任务分配表 `docs/迭代1-开发任务分配.md`
- 项目初始化：前端 `miniprogram/` + 后端 `server/`
- 目录结构规范制定

### 研发-A — 后端核心服务
- **工作内容**:
  - 数据库 Prisma Schema（7张表：User/Address/DailyUsage/Photo/Model/Order/OrderItem）
  - Koa2 服务入口 + 中间件（JWT 鉴权、错误处理、请求日志）
  - 用户服务：微信 code2Session 登录、手机号快速验证绑定、个人信息 CRUD
  - 地址服务：CRUD + 默认地址管理
  - 统一 API 格式：`{ code: 0, data: {}, message: "" }`
- **新建文件**:
  - `server/prisma/schema.prisma`
  - `server/src/app.ts`
  - `server/src/types/index.ts`
  - `server/src/middlewares/auth.ts`、`error.ts`、`logger.ts`
  - `server/src/services/user.ts`、`address.ts`
  - `server/src/routes/user.ts`、`address.ts`

### 研发-B — 前端个人信息 & 地址管理
- **工作内容**:
  - 个人信息编辑页（头像/昵称/手机号绑定）
  - 收货地址列表页（展示/设默认/编辑/删除）
  - 收货地址编辑页（新增/编辑表单+省市区选择器+删除）
- **新建文件**:
  - `miniprogram/pages/profile-edit/`（wxml/wxss/ts/json）
  - `miniprogram/pages/address-list/`（wxml/wxss/ts/json）
  - `miniprogram/pages/address-edit/`（wxml/wxss/ts/json）

### 研发-C — 前端页面 UI
- **工作内容**:
  - 欢迎页（品牌展示+微信登录按钮+协议勾选）
  - 首页（Banner轮播+开始定制入口+制作流程图+精选展示）
  - 个人中心（用户卡片+订单快捷入口+模型缩略+功能列表+退出登录）
  - 定制Tab占位页
  - Tab栏配置（首页/定制/我的）
- **新建文件**:
  - `miniprogram/pages/welcome/`（wxml/wxss/ts/json）
  - `miniprogram/pages/home/`（wxml/wxss/ts/json）
  - `miniprogram/pages/profile/`（wxml/wxss/ts/json）
  - `miniprogram/pages/customize/`（wxml/wxss/ts/json）

### 公共文件
- `miniprogram/app.ts` — 小程序入口
- `miniprogram/app.json` — 页面路由+Tab栏
- `miniprogram/app.wxss` — 全局样式+CSS变量
- `miniprogram/utils/request.ts` — 网络请求封装
- `miniprogram/utils/auth.ts` — 登录态管理
- `miniprogram/utils/storage.ts` — 本地缓存工具
- `miniprogram/package.json` — 前端依赖（TDesign/MobX）
- `server/package.json`、`server/tsconfig.json`、`server/.env.example`
- `README.md` — 项目说明

---

## 迭代 2：照片上传 + AI 3D 模型生成

### 研发-A — 后端 AI 生成服务
- **工作内容**:
  - 腾讯云 COS 文件上传/删除/签名URL服务
  - 照片 CRUD + 每日生成次数管理（2次/天，失败回退）
  - AI 3D 生成服务（Tripo3D API 对接 + 轮询 + 失败分类 + 重试）
  - 文件上传路由（multipart 处理）
- **新建文件**:
  - `server/src/services/cos.ts`、`photo.ts`、`model.ts`
  - `server/src/routes/photo.ts`、`model.ts`、`upload.ts`
- **修改文件**:
  - `server/src/app.ts` — 注册新路由、替换为 koa-body
  - `server/package.json` — 新增 COS/koa-body/bullmq/mime-types 依赖

### 研发-B/C — 前端照片上传与 AI 生成流程
- **工作内容**:
  - 拍照引导页（正确/错误示范、拍照要点、"不再提示"缓存）
  - 照片上传页（拍照/相册、历史照片、次数展示、长按删除）
  - 照片裁剪页（3:5裁剪框、旋转/重置、Canvas裁剪导出）
  - AI生成等待页（脉冲动画、进度条、状态轮询3s、订阅消息）
  - 生成失败页（4种失败分类、重试3次上限、换照片、查看引导）
- **新建文件**:
  - `miniprogram/pages/photo-guide/`（wxml/wxss/ts/json）
  - `miniprogram/pages/photo-upload/`（wxml/wxss/ts/json）
  - `miniprogram/pages/photo-crop/`（wxml/wxss/ts/json）
  - `miniprogram/pages/generate-waiting/`（wxml/wxss/ts/json）
  - `miniprogram/pages/generate-failed/`（wxml/wxss/ts/json）
- **修改文件**:
  - `miniprogram/app.json` — 注册5个新页面路由
  - `miniprogram/pages/home/home.ts` — "开始定制"按钮跳转拍照流程

---

## 迭代 3：3D 预览 + 产品定制

### 后端新增
- **工作内容**:
  - 产品配置服务（手办尺寸/打印类型/包装盒/套餐定义/价格计算）
  - 产品配置 API（获取配置/计算价格）
  - 模型详情/删除接口
- **新建文件**:
  - `server/src/services/product.ts` — 产品配置与定价服务
  - `server/src/routes/product.ts` — 产品配置路由
- **修改文件**:
  - `server/src/app.ts` — 注册产品路由
  - `server/src/services/model.ts` — 新增 deleteModel/getModelDetail
  - `server/src/routes/model.ts` — 新增 DELETE/详情接口

### 前端新增页面（3个）
- **3D模型预览页** (`miniprogram/pages/model-preview/`):
  - WebGL Canvas 3D 渲染 + 降级静态预览
  - 手势交互：单指旋转360°、双指缩放(0.5x-3x)、双击重置
  - 自动旋转(15°/s)、垂直限角(-30°~+60°)
  - 首次手势引导蒙层（3条引导 + 不再提示缓存）
  - 导出模型弹窗（保存到手机/微信发送）
  - 分享面板（转发好友/生成海报）
  - 底部操作：导出模型 / 重新生成 / 定制我的手办

- **我的模型列表页** (`miniprogram/pages/model-list/`):
  - 两列网格布局、3:4卡片比例
  - 已完成/生成中/失败三种状态展示
  - 长按操作菜单（定制手办/删除）
  - 空状态引导（去上传照片）
  - 下拉刷新

- **产品定制页** (`miniprogram/pages/product-customize/`):
  - 模型信息头部 + 查看3D预览入口
  - 推荐套餐区（基础款/精致款/豪华款）
  - 手办尺寸必选（S/M/L 单选卡片）
  - 可选周边：模型照片打印 / 真人照片打印 / 专属包装盒
  - 打印类型单选（相纸/卡片/亚克力）
  - 包装盒8色选择
  - 套餐与自由定制双向联动匹配
  - 前端实时价格计算（200ms内）
  - 底部常驻价格栏 + 提交订单按钮
  - 价格明细弹窗

### 修改已有页面
- `miniprogram/pages/generate-waiting/generate-waiting.ts` — 生成完成跳转到3D预览页
- `miniprogram/pages/profile/profile.ts` — 模型列表/详情跳转功能
- `miniprogram/app.json` — 注册3个新页面路由

### 新增后端 API（3个）
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/product/config` | 获取产品配置 |
| POST | `/api/product/calculate` | 价格计算 |
| GET | `/api/model/:id/detail` | 模型详情 |
| DELETE | `/api/model/:id` | 删除模型 |

---

## 迭代 4：订单支付 + 收尾

### 后端新增
- **工作内容**:
  - 订单服务（创建/列表/详情/取消/确认收货/退款申请）
  - 模拟支付（MVP阶段不接真实微信支付，¥0.01测试价）
  - 订单编号生成（YN + yyyyMMdd + 6位序号）
  - 订单超时自动取消（30分钟）
  - 退款时限校验（支付后24h/收货后7天）
- **新建文件**:
  - `server/src/services/order.ts` — 订单核心服务
  - `server/src/routes/order.ts` — 订单路由
- **修改文件**:
  - `server/src/app.ts` — 注册订单路由、补全产品路由import

### 前端新增页面（5个）
- **订单确认页** (`miniprogram/pages/order-confirm/`):
  - 收货地址展示/修改/添加
  - 商品信息（手办 + 附加商品清单）
  - 订单备注（200字）
  - 价格明细（合计/套餐优惠/运费/应付）
  - 测试价提示
  - 底部常驻微信支付按钮（绿色）
  - 支付中loading遮罩

- **支付成功页** (`miniprogram/pages/order-success/`):
  - 成功图标 + 订单关键信息（编号/金额/时间）
  - 制作时间提示（7-15工作日）
  - 三个入口：查看订单/继续定制/返回首页
  - 分享按钮（open-type="share"）
  - 拦截返回（redirectTo不回确认页）

- **订单详情页** (`miniprogram/pages/order-detail/`):
  - 7种状态顶部区域（渐变色背景+图标+描述）
  - 待支付倒计时（mm:ss秒级刷新）
  - 收货信息/商品信息/物流信息/价格信息/订单信息
  - 复制订单号/快递号
  - 底部操作栏随状态变化：
    待支付=[取消订单][立即支付]
    制作中=[联系客服]
    已发货=[联系客服][确认收货]
    已完成=[联系客服][申请退款][再来一单]
    已取消=[重新下单]

- **订单列表页** (`miniprogram/pages/order-list/`):
  - Tab筛选（全部/待支付/制作中/已发货/已完成）
  - 订单卡片：编号+状态标签+商品缩略图+概要+价格+时间
  - 状态标签颜色（橙/灰/蓝/绿/黑）
  - 卡片操作按钮（取消/支付/收货/再来一单/重新下单）
  - 下拉刷新 + 上拉分页（每页10条）
  - 空状态引导

- **退款申请页** (`miniprogram/pages/refund-apply/`):
  - 订单信息（编号/金额）
  - 退款原因4选项单选
  - 退款说明文本输入（200字）
  - 上传凭证（最多3张照片）
  - 退款须知提示
  - 底部提交按钮（未选原因时置灰）

### 修改已有页面
- `miniprogram/pages/product-customize/product-customize.ts` — 提交订单跳转订单确认页
- `miniprogram/pages/profile/profile.ts` — 订单列表跳转已连通
- `miniprogram/app.json` — 注册5个新页面路由

### 新增后端 API（7个）
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/order/create` | 创建订单 |
| POST | `/api/order/:id/pay` | 模拟支付 |
| GET | `/api/order/list` | 订单列表（分页+状态筛选）|
| GET | `/api/order/:id/detail` | 订单详情 |
| POST | `/api/order/:id/cancel` | 取消订单 |
| POST | `/api/order/:id/confirm` | 确认收货 |
| POST | `/api/order/:id/refund` | 申请退款 |

---

## 迭代 5：联调部署 + 测试号适配

> 目标：将项目跑通联调，支持测试号模式开发，后端部署到腾讯云服务器，手机真机可测试

### PM（组长）工作
- 协调联调部署流程，解决环境差异问题
- 服务器部署方案选型（腾讯云轻量应用服务器）
- 端口冲突处理（Docker 占用 3000 → 改用 3001）
- 整体进度把控：前端编译 → 后端部署 → 真机验证

### 研发-A — 后端服务改造与部署
- **工作内容**:
  - 新增 `POST /api/user/dev-login` 开发模式免登录接口（绕过微信 code2Session）
  - `server/src/app.ts` 添加全局未捕获异常处理（防止进程崩溃退出）
  - `server/src/app.ts` 添加静态文件服务（`/uploads/` 路径，开发模式图片存储）
  - `server/src/routes/upload.ts` 重写上传接口：COS 模式改为本地文件存储模式
  - 后端部署到腾讯云服务器（`43.138.185.112:3001`）
  - 服务器环境搭建：Node.js 20 + npm install + prisma generate
- **修改文件**:
  - `server/src/routes/user.ts` — 新增 dev-login 路由
  - `server/src/routes/upload.ts` — 重写为本地存储模式
  - `server/src/app.ts` — 异常兜底 + 静态文件服务
  - `server/.env` — 端口改为 3001
- **服务器部署信息**:
  - 路径：`/opt/yours-server/`
  - 端口：3001（3000 被 Docker 占用）
  - 重启：`cd /opt/yours-server && sudo nohup node dist/app.js > server.log 2>&1 &`
  - 日志：`cat /opt/yours-server/server.log`

### 研发-B — 前端测试号适配与编译
- **工作内容**:
  - `miniprogram/utils/auth.ts` 新增 `isDevMode()` / `devLogin()` 函数
  - `miniprogram/utils/auth.ts` 修改 `checkLoginStatus()` 开发模式不依赖 `wx.checkSession`
  - `miniprogram/pages/welcome/welcome.ts` 登录逻辑适配（开发模式 / 正式模式自动切换）
  - `miniprogram/app.ts` baseUrl 从 `localhost` → 局域网 IP → 公网 IP `43.138.185.112:3001`
- **修改文件**:
  - `miniprogram/utils/auth.ts` — 新增 devLogin + isDevMode
  - `miniprogram/pages/welcome/welcome.ts` — 登录流程适配
  - `miniprogram/app.ts` — baseUrl 指向公网

### 研发-C — 前端编译环境修复
- **工作内容**:
  - 解决微信开发者工具 TypeScript 编译插件 bug（`getDevCodeByFileList` 报错）
  - 关闭工具 TS 插件，改为手动 `npx tsc` 编译
  - `tsconfig.json` module 从 ESNext 改为 CommonJS（小程序运行时需要）
  - 引入 `miniprogram-api-typings` 类型定义
  - 修复 4 个 TS 类型错误（WebGLRenderingContext / saveFile / unknown / GetPhoneNumberEvent）
  - 手动构建 `miniprogram_npm`（TDesign 组件拷贝）
  - 修复 `app.wxss` TDesign 样式引用路径（`common/index.wxss` → `common/style/index.wxss`）
  - 生成 TabBar 图标和微信白色图标占位文件
- **修改文件**:
  - `miniprogram/tsconfig.json` — module + types 配置
  - `miniprogram/project.config.json` — 关闭 TS 编译插件
  - `miniprogram/app.wxss` — TDesign 样式路径修正
  - `miniprogram/pages/model-preview/model-preview.ts` — 类型修复
  - `miniprogram/pages/product-customize/product-customize.ts` — 类型修复
  - `miniprogram/pages/profile-edit/profile-edit.ts` — 类型修复
  - `miniprogram/pages/photo-crop/photo-crop.wxss` — 裁剪页拖动修复

### 研发-D — 裁剪页交互修复
- **工作内容**:
  - 修复照片裁剪页 `movable-view` 无法拖动的问题
  - 根因：`.crop-bg` 使用 `height:0 + padding-bottom` 导致 `movable-area` 触摸区域为 0
  - 修复：改为 `height: 800rpx` 固定高度，添加 z-index 分层
- **修改文件**:
  - `miniprogram/pages/photo-crop/photo-crop.wxss` — 裁剪区域布局修复

### 新增后端 API（1个）
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/user/dev-login` | 开发模式免登录（返回测试用户 token） |

### 环境配置变更
| 项目 | 变更前 | 变更后 |
|------|--------|--------|
| 后端运行位置 | 本地 localhost:3000 | 腾讯云 43.138.185.112:3001 |
| 小程序 baseUrl | `http://localhost:3000/api` | `http://43.138.185.112:3001/api` |
| TS 编译方式 | 微信工具插件编译 | 手动 `npx tsc` |
| 文件上传方式 | COS（未配置） | 服务器本地存储 `/uploads/` |
| 登录方式 | wx.login + code2Session | devLogin 免登录（开发模式） |

---

## 迭代 6：真机测试 Bug 修复

> 目标：修复真机测试中发现的 UI 和交互问题

### 研发-D — 3D模型预览页空白修复
- **问题描述**: 真机进入模型预览页后整个页面空白，只有底部"导出模型/重新生成/定制我的手办"按钮可见
- **根因分析**:
  1. `initWebGL()` → `setupScene()` 只做了 `gl.clear()`（清屏为灰色），没有实际加载和渲染 3D 模型（注释写"待接入 Three.js glTF 加载器"）
  2. wxml 中没有预览图 `<image>` 降级方案，当 WebGL 无内容时整个区域为空
  3. `loading` 状态在 WebGL 初始化后设为 false，但没有任何可见内容显示
- **修复方案**: 移除空 WebGL Canvas，改为**预览图 + CSS 3D 旋转效果**降级方案
- **修改文件**:
  - `miniprogram/pages/model-preview/model-preview.wxml` — 用 `<image>` + 3D transform 替代空的 `<canvas type="webgl">`
  - `miniprogram/pages/model-preview/model-preview.ts` — 移除 `initWebGL()`/`setupScene()`，加载数据后直接启动自动旋转
  - `miniprogram/pages/model-preview/model-preview.wxss` — 新增预览图容器/旋转底座/指示器样式

### 研发-B — 裁剪页确认后跳回登录页修复
- **问题描述**: 选择照片裁剪确认后选择 AI 引擎，会直接跳转回登录页
- **根因分析**:
  1. **HTTP 方法不匹配**：`photo-crop.ts` 第91行用 `post('/photo/${photoId}/crop')` 调用后端，但后端 `photo.ts` 定义的是 `PUT /api/photo/:id/crop`，方法不匹配导致请求失败
  2. **401 跳转无提示**：`request.ts` 收到 401 时直接 `wx.reLaunch` 跳转到登录页，没有任何 toast 提示，用户无法分辨是 token 过期还是其他错误
  3. **引擎选择取消误跳转**：`showEngineSelector` 中用户取消 ActionSheet 时 `fail` 回调里仍然 `wx.redirectTo` 到生成页（使用默认引擎），应该留在裁剪页
- **修复内容**:
  - `miniprogram/pages/photo-crop/photo-crop.ts` — import `put`，第91行 `post` → `put`；引擎选择取消不跳转
  - `miniprogram/utils/request.ts` — 401 跳转前增加 toast 提示 + console 调试日志 + 1.5s 延迟跳转；非200请求增加错误日志

### 研发-B — 生成失败后自动跳回登录页修复（全链路）
- **问题描述**: AI 生成失败后展示了失败页面，但随即自动跳转回登录页。第一轮仅修复轮询 `silent401` 后问题依旧。
- **根因分析（深入排查）**:
  1. **轮询请求**（已修复）：`checkStatus()` 的 `GET /model/:id/status` 已有 `silent401: true`
  2. **生成请求缺口**：`startGenerate()` 的 `POST /model/generate` **没有** `silent401`，401 时触发非静默处理 → `wx.removeStorageSync('token')` + `setTimeout(reLaunch, 1500)` → 即使 catch 中 `redirectTo` 到失败页，1.5s 后 `setTimeout` 仍执行 `reLaunch` 覆盖当前页
  3. **重试请求缺口**：`generate-failed.ts` 的 `doRetry()` 中 `POST /model/:id/retry` 也没有 `silent401`，401 时同样触发全局跳转
  4. **上传请求缺口**：`photo-crop.ts` 的 `uploadToServer()` 直接用 `wx.uploadFile`（不走 `request.ts`），401 未被检测
  5. **裁剪接口请求缺口**：`photo-crop.ts` 的 `post('/photo', ...)` 和 `put('/photo/:id/crop', ...)` 也没有 `silent401`
  6. **setTimeout 不可取消**：非静默 401 中 `setTimeout(() => reLaunch, 1500)` 一旦设置，即使页面已经跳走也不可取消
  7. **401 防重入缺失**：多个并发请求可能同时触发 401 处理，导致重复清除 token 和重复跳转
- **修复内容（全链路）**:
  - `miniprogram/pages/generate-waiting/generate-waiting.ts` — `startGenerate()` 的 `POST /model/generate` 添加 `{ silent401: true }`，catch 中自行处理 TOKEN_EXPIRED
  - `miniprogram/pages/generate-failed/generate-failed.ts` — `doRetry()` 的 `POST /model/:id/retry` 添加 `{ silent401: true }`，catch 中自行处理 TOKEN_EXPIRED
  - `miniprogram/pages/photo-crop/photo-crop.ts` — `uploadToServer()` 中 `wx.uploadFile` 增加 `res.statusCode === 401 || data.code === 401` 检测，返回 `TOKEN_EXPIRED` 错误；`post('/photo')` 和 `put('/photo/:id/crop')` 均添加 `{ silent401: true }`；`onConfirm()` catch 中统一处理 TOKEN_EXPIRED
  - `miniprogram/utils/request.ts` — 新增全局 `isHandling401` 防重入标记，防止多个请求同时触发 401 时重复清除 token 和重复跳转；非静默 401 的 reject 也统一使用 `TOKEN_EXPIRED` 错误消息

### 研发-A — model 路由 koa-router 中间件注册 Bug 修复（真正根因）
- **问题描述**: `POST /api/model/generate` 和 `POST /api/model/:id/retry` 始终返回 401，即使 token 完全有效。同一个 token 请求 `GET /api/model/list` 正常200。
- **根因分析**: 通过编写 API 测试脚本，发现 `/api/model` 路由下**所有 POST 请求返回 401，但所有 GET 请求正常**。原因是 koa-router 的 `router.use(authMiddleware)` 在 `router.get('/engines', ...)` 之后注册时，对后续 POST 路由的中间件应用存在兼容性问题（koa-router 的 `router.use()` 对 GET/POST 方法路由的处理不一致）。
- **修复方案**: 移除 `router.use(authMiddleware)` 全局中间件注册方式，改为**每个路由单独内联 `authMiddleware`**（如 `router.post('/generate', authMiddleware, async (ctx) => {...})`），确保鉴权中间件对所有 HTTP 方法都正确生效。
- **修改文件**: `server/src/routes/model.ts` — 7个路由全部改为单独内联 authMiddleware
- **验证方法**: `node test-api4.js` 对比测试（部署后需重新验证）

### 研发-A — auth.ts `await next()` 位置导致假 401 修复
- **问题描述**: `POST /api/model/generate` 始终返回 401，即使 token 完全有效且 model 路由已改为内联 authMiddleware
- **根因分析**: `server/src/middlewares/auth.ts` 中 `await next()` 放在 JWT 验证的 `try-catch` 块内，导致后续路由处理器（如 `prisma.dailyUsage.upsert()`）抛出的异常被 auth 的 catch 捕获，错误地返回 401
- **修复方案**: 将 `await next()` 从 JWT try-catch 块中移出，放在 catch 之后。JWT 验证失败走 catch + return，通过后执行 `await next()`，后续路由异常不再被误判为 401
- **修改文件**: `server/src/middlewares/auth.ts` — 分离 JWT 验证和路由执行的 try-catch
- **验证结果**: `node verify.js` 确认 model/generate 不再返回 401（改为返回 500 — Prisma 数据库错误）

### 研发-A — dailyUsage.upsert() 唯一约束冲突修复
- **问题描述**: auth.ts 修复后，`POST /api/model/generate` 返回 500：`Unique constraint failed on the constraint: daily_usage_user_id_usage_date_key`
- **根因分析**: `prisma.dailyUsage.upsert()` 在 MySQL `DATE` 类型字段上因 JavaScript Date 对象的时区问题导致唯一约束冲突。`new Date().setHours(0,0,0,0)` 得到本地时区（UTC+8）午夜，Prisma 传给 MySQL 时转为 UTC（前一天 16:00），导致 upsert 的 where 查不到已有记录 → 走 create → 唯一约束冲突
- **修复方案**:
  1. 新增 `getTodayDate()` 工具函数：用 `YYYY-MM-DDT00:00:00.000Z` 格式构造 UTC 零点日期，确保与 MySQL DATE 存储一致
  2. 将 `consumeUsage()` 从 `prisma.upsert()` 改为 **先查后改**：`findUnique` → `update`/`create`
  3. `create` 外层加 try-catch 捕获 P2002（唯一约束冲突），处理并发竞争 → fallback 到 update
  4. `getDailyUsage()` 和 `refundUsage()` 同步使用 `getTodayDate()`
- **修改文件**: `server/src/services/photo.ts` — 重写 consumeUsage/getDailyUsage/refundUsage + 新增 getTodayDate