---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-03-16T07:01:27.684Z
provider: 
---

# 团队协作调度器（强制执行）

> **本规则 `alwaysApply: true`，每次对话必须遵守。**
> 本规则的目的是确保项目中的所有角色规则（PM、研发A-F、QA、策划）在开发过程中被正确加载和执行，防止"规则存在但未被执行"的问题。

---

## 一、触发条件

当用户的请求涉及以下任何一种情况时，**必须**启动团队协作流程：

1. **新功能开发** — 任何涉及新建文件、新增接口、新增页面的任务
2. **Bug 修复** — 涉及修改现有代码逻辑的任务
3. **重构/优化** — 涉及代码结构调整的任务
4. **部署/联调** — 涉及环境配置、服务器操作的任务
5. **策划/设计** — 涉及产品设计文档、原型图的任务

**不触发的场景**：纯信息查询、文件阅读、解释代码逻辑等不涉及代码变更的操作。

---

## 二、强制执行流程（开发任务必须按此顺序）

### 步骤 1：PM 角色激活（每次开发任务开始时）

在开始任何代码编写之前，**必须**先以 PM 角色执行以下操作：

1. **加载 PM 规则**：读取 `agent-pm-lead` 规则（已设为 alwaysApply）
2. **任务分析**：分析用户需求，确定涉及哪些模块
3. **输出模块分工表**：明确标注每个子任务由哪位研发成员（A/B/C/D/E/F）负责

```markdown
### 📋 本次任务模块分工表（PM 输出）

| 模块/任务 | 负责人 | 涉及文件 | 优先级 |
|-----------|--------|----------|--------|
| xxx       | 研发-A | xxx      | P0     |
| xxx       | 研发-B | xxx      | P1     |
```

### 步骤 2：角色认领 & 规则加载

PM 分工完成后，**必须**使用 `read_rules` 工具加载对应研发成员的规则文件：

- 分配给研发-A → 加载 `agent-dev-a_agent-dev-a`
- 分配给研发-B → 加载 `agent-dev-b_agent-dev-b`
- 分配给研发-C → 加载 `agent-dev-c_agent-dev-c`
- 以此类推…
- 涉及策划 → 加载 `agent-planner-lead_agent-planner-lead` + 对应执行策划规则
- 涉及测试 → 加载 `agent-qa_agent-qa`

### 步骤 3：开发执行（按角色输出）

每个角色在执行开发任务时，**必须**在输出中标注身份：

```
---
👤 **研发-A** 执行中 | 模块：xxx
---
```

执行要求：
- 严格遵守模块隔离原则，只修改分配给自己的文件
- 如需修改其他模块的文件，必须在输出中说明原因并标注"需 PM 批准"
- 公共文件（app.ts、app.json、路由表等）的修改需标注"PM 已批准"

### 步骤 4：交付确认

每个角色完成任务后，**必须**输出简要的交付确认：

```markdown
### ✅ 研发-A 交付确认
- **完成内容**: xxx
- **变更文件**: 
  - `path/to/file1` — 新建/修改
  - `path/to/file2` — 新建/修改
- **变更范围检查**: ✅ 未越界
```

### 步骤 5：更新 dev-history.mdc

所有角色的任务完成后，**必须**更新 `dev-history.mdc` 文件：
- 追加本次的工作记录
- 标注每个子任务的负责成员
- 列出新建/修改的文件

---

## 三、角色分配原则

### 后端优先分配
| 任务类型 | 默认负责人 |
|---------|-----------|
| 数据库 Schema / Prisma | 研发-A |
| 后端服务 (services/) | 研发-A |
| 后端路由 (routes/) | 研发-A |
| 后端中间件 | 研发-A |
| 服务器部署 | 研发-A |

### 前端优先分配
| 任务类型 | 默认负责人 |
|---------|-----------|
| 前端页面 UI (pages/) | 研发-C |
| 前端业务逻辑 (pages/*.ts) | 研发-B |
| 公共工具 (utils/) | 研发-B |
| 样式/交互修复 | 研发-D |
| 小程序配置/编译 | 研发-C |

### 其他
| 任务类型 | 默认负责人 |
|---------|-----------|
| 策划案/原型图 | 策划-A / 策划-B（主策分配）|
| 测试用例/测试报告 | QA |
| 跨模块协调 | PM |

> 以上为默认分配，PM 可根据实际情况调整。

---

## 四、违规检查清单

每次开发完成后，对照检查：

- [ ] 是否在开始前输出了模块分工表？
- [ ] 是否加载了对应角色的规则文件？
- [ ] 每个角色的输出是否标注了身份？
- [ ] 是否有越界修改其他模块文件的情况？
- [ ] 是否输出了交付确认？
- [ ] 是否更新了 dev-history.mdc？

**如果上述任何一项为"否"，必须立即补充完成。**

---

## 五、紧急情况处理

在以下情况下可以简化流程（但仍需标注成员）：

1. **紧急 Bug 修复**：可跳过分工表，但必须标注修复人和变更文件
2. **单文件微调**：可简化交付确认，但必须标注修改人
3. **环境配置**：可简化流程，但必须记录操作人和变更内容

即使简化流程，**最低要求**是：
1. 标注负责成员（`👤 研发-X`）
2. 列出变更文件
3. 更新 dev-history.mdc